import 'package:flutter/material.dart';

class DressDetails extends StatelessWidget {
  const DressDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('example'),
    );
  }
}
